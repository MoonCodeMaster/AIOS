You are AIOS. Turn this brainstorm transcript into a project spec.

TRANSCRIPT:
{{.Transcript}}

Output ONLY the contents of .aios/project.md (with YAML frontmatter):
- `name`, `goal`, `non_goals`, `constraints`, `acceptance_bar`
- Body: sections for Architecture sketch and Open questions

No preamble. No trailing commentary.
